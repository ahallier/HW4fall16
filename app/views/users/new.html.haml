%h1 Sign-up

= form_tag new_user_path do

  = label :user, :user_id, 'User ID'
  = text_field :user, 'user id'
  
  = label :user, :email, 'Email'
  = text_field :user, 'email'

  = submit_tag 'Save'
